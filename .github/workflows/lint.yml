# Every check that runs against your pull request, in one place.
#
# This file used to be two: skills-lint and the validate half of
# learning-promote. They had identical triggers and did the same kind of work
# -- read the tree, fail if it contradicts itself -- but appeared as two
# separate entries in the Actions tab, which made the harness look like it had
# more moving parts than it does.
#
# Job NAMES are load-bearing. A required status check resolves by job name, not
# by file name, so `Governance lint` must keep that exact name or the branch
# ruleset silently waits forever for a check that no longer exists.
name: lint

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:

  governance:
    # This name is the required status check context in the branch ruleset.
    # Renaming it silently detaches the gate, so the ruleset must be updated
    # in the same change.
    name: Governance lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: SKILL.md files have required frontmatter
        run: |
          set -euo pipefail
          python3 -m pip install --quiet pyyaml
          fail=0
          # A SKILL.md is how an agent decides what to do, so the shape of one
          # is a harness rule and applies to every skill in the repository --
          # including the use case's own. The use case root comes from the
          # manifest rather than a literal path: this step must not know which
          # domain it is linting, only that skills must be well formed.
          UC_ROOT=$(./harness/usecase root)
          SKILLS=$(find skills "$UC_ROOT/skills" -name SKILL.md 2>/dev/null | sort)
          [ -n "$SKILLS" ] || { echo "::error::no SKILL.md files found at all"; exit 1; }
          for f in $SKILLS; do
            echo "--- $f"
            if [ "$(head -1 "$f")" != "---" ]; then
              echo "::error file=$f::missing YAML frontmatter opening delimiter"; fail=1; continue
            fi
            fm=$(awk 'NR>1{if($0=="---") exit; print}' "$f")
            for key in name description; do
              if ! grep -q "^$key:" <<<"$fm"; then
                echo "::error file=$f::frontmatter missing required key '$key'"; fail=1
              fi
            done
            # A description that does not say WHEN to use the skill is not
            # actionable for an agent choosing between skills.
            desc=$(grep '^description:' <<<"$fm" | cut -d: -f2- | sed 's/^ *//')
            if [ "${#desc}" -lt 40 ]; then
              echo "::error file=$f::description too short to be useful (${#desc} chars)"; fail=1
            fi
            if ! grep -qi 'use when\|use whenever' <<<"$desc"; then
              echo "::error file=$f::description must state when to use the skill"; fail=1
            fi
          done
          exit $fail

      - name: Required governance files exist
        run: |
          set -euo pipefail
          fail=0
          for f in AGENTS.md GOVERNANCE.md .github/CODEOWNERS \
                   .github/copilot-instructions.md CLAUDE.md; do
            [ -f "$f" ] || { echo "::error::missing required file $f"; fail=1; }
          done
          exit $fail

      - name: Vendor pointer files contain no rules of their own
        run: |
          set -euo pipefail
          fail=0
          # Pointer files must redirect to AGENTS.md. If rules accumulate in
          # vendor-specific files the contract stops being model-independent,
          # which is the whole premise of the harness.
          for f in .github/copilot-instructions.md CLAUDE.md; do
            grep -q 'AGENTS.md' "$f" || { echo "::error file=$f::must reference AGENTS.md"; fail=1; }
            lines=$(grep -cve '^\s*$' "$f")
            if [ "$lines" -gt 15 ]; then
              echo "::error file=$f::pointer file has $lines lines; rules belong in AGENTS.md"; fail=1
            fi
          done
          exit $fail

      - name: Harness / use case boundary
        run: |
          python3 -m pip install --quiet pyyaml
          ./harness/check_boundary

      - name: Non-negotiable rules are intact
        run: |
          set -euo pipefail
          fail=0
          # Agents may propose changes to AGENTS.md. They may not remove the
          # rules that keep a human in control. This is the backstop for that.
          declare -a required=(
            "Never commit directly to"
            "Never merge a pull request"
            "Never approve a pull request"
            "Never weaken a gate"
            "Never introduce a floating version"
          )
          for rule in "${required[@]}"; do
            if ! grep -qi "$rule" AGENTS.md; then
              echo "::error file=AGENTS.md::non-negotiable rule removed or reworded: '$rule'"
              fail=1
            fi
          done
          # GOVERNANCE.md must keep asserting human-only merge authority.
          grep -qi 'only.*human\|human.*only' GOVERNANCE.md || {
            echo "::error file=GOVERNANCE.md::human-only merge authority no longer asserted"; fail=1; }
          exit $fail

      - name: No workflow may merge or approve
        run: |
          set -euo pipefail
          fail=0
          # Layer 3 of the enforcement model (GOVERNANCE.md §2), checked
          # mechanically so it cannot regress unnoticed.
          #
          # This file is excluded from the scan because it necessarily contains
          # the very patterns it searches for. The exclusion names this file,
          # so it must be updated if the file is ever renamed -- when the three
          # lint workflows merged into `lint.yml` the stale `skills-lint.yml`
          # exclusion would have made this gate match its own source and fail
          # every PR. Verified after the rename that the gate still passes on a
          # clean tree and still fires on a planted violation.
          if grep -rn --exclude=lint.yml \
               -e 'gh pr merge' -e 'pulls/.*/merge' -e 'enable-pull-request-automerge' \
               .github/workflows/ 2>/dev/null; then
            echo "::error::a workflow attempts to merge pull requests"; fail=1
          fi
          if grep -rnE --exclude=lint.yml \
               -e 'event[:=][[:space:]]*.?APPROVE' \
               -e '--approve' \
               -e 'pr[[:space:]]+review[^|]*--approve' \
               .github/workflows/ 2>/dev/null; then
            echo "::error::a workflow attempts to APPROVE pull requests"; fail=1
          fi
          exit $fail

      - name: Workflow YAML parses
        run: |
          set -euo pipefail
          # A workflow that does not parse is a silently disabled gate. Since
          # agents author workflows here, this must be checked mechanically.
          fail=0
          while read -r f; do
            python3 -c "import sys,yaml; yaml.safe_load(open(sys.argv[1])); print('ok   '+sys.argv[1])" "$f" || { echo "FAIL $f"; fail=1; }
          done < <(find .github/workflows -name '*.yml')
          exit $fail

      # Language-specific syntax checking belongs to whoever owns the language.
      # This job checks only what every repository has: shell and YAML.
      - name: Shell scripts parse
        run: |
          set -euo pipefail
          fail=0
          while read -r f; do
            bash -n "$f" && echo "ok   $f" || { echo "FAIL $f"; fail=1; }
          done < <(grep -rl '^#!/usr/bin/env bash' harness tools 2>/dev/null)
          exit $fail

  validate:
    name: Harness self-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.4.2"
          use-public-rspm: true

      - name: Install jsonlite
        run: Rscript -e 'install.packages("jsonlite", repos="https://cloud.r-project.org")'

      - name: Validate the learning log
        run: Rscript harness/learnings.R validate evidence/learnings.jsonl

      # The aggregator decides whether the research loop can produce anything
      # at all. It spent its whole life returning "no candidate cleared the
      # gates" while every surrounding job reported success, because nothing
      # asserted that a well-formed week SHOULD yield a proposal. These tests
      # make that assertion, so the flywheel cannot quietly stop turning again.
      - name: Research aggregator tests
        run: Rscript harness/research/test_aggregate.R

      - name: The generated block must match the log
        run: |
          set -euo pipefail
          cp AGENTS.md /tmp/AGENTS.before
          Rscript harness/promote_learnings.R evidence/learnings.jsonl AGENTS.md 2 >/dev/null
          if ! diff -q /tmp/AGENTS.before AGENTS.md >/dev/null; then
            echo "::error file=AGENTS.md::The recurring-lessons block does not match evidence/learnings.jsonl."
            echo "It is generated. Run: Rscript harness/promote_learnings.R evidence/learnings.jsonl AGENTS.md 2"
            diff /tmp/AGENTS.before AGENTS.md || true
            exit 1
          fi
          echo "Generated block is in sync with the log."

      - name: Summary
        run: Rscript harness/learnings.R summary evidence/learnings.jsonl >> "$GITHUB_STEP_SUMMARY"
